git clone https://github.com/StephaneAcode/Python.git
#git clone git@github.com:StephaneAcode/Python.git

git fetch

git status

git add <file>

git commit -m "message"

ou

git -a (--all) -m "message"

git log

git push

#Aller sur le dernier commit après un fetch en discardant les changements locaux
git reset --hard origin/master
