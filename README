git clone https://github.com/StephaneAcode/Python.git
#git clone git@github.com:StephaneAcode/Python.git

git fetch

git status

git add <file>

git commit -m "message"

ou

git -a (--all) -m "message"

git log

git push